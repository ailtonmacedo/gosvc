apiVersion: v1
kind: Service
metadata:
  name: {{.Config.Project.Name}}
  namespace: {{.Config.Deployment.Namespace}}
spec:
  selector: { app: {{.Config.Project.Name}}, component: api }
  ports: [{ name: http, port: 80, targetPort: http }]
